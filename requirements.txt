import sys
import subprocess
try:
    import kaleido
except ImportError:
    subprocess.run([sys.executable, "-m", "pip", "install", "kaleido"])
    import kaleido

# Core framework
streamlit==1.38.0

# Data science
pandas>=2.2.0
numpy>=1.26.0
scikit-learn>=1.4.0
joblib>=1.3.2

# Machine Learning Models
prophet>=1.1.5
xgboost>=2.0.3

# Visualization
plotly==5.24.1
matplotlib>=3.8.0
kaleido>=0.2.1     # ✅ จำเป็นสำหรับ fig.write_image() (ใช้ตอน export PDF)

# PDF Export
reportlab>=4.0.9   # ✅ ต้องใช้เวอร์ชัน >=4.0.9 เพื่อรองรับ UnicodeCIDFont (ภาษาไทย)
